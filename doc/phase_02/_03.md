# Phase 02-3: ReAct 그래프 문제점 분석 및 개선

## 1. 발견된 문제점

### 1.1 텍스트 깨짐 현상

**증상:**
```
[{'type': 'text', 'text': '오늘은 2월 4일입니다.', 'extras': {'signature': 'CqECAXLI2nx21fxt...'}}]
```

**원인:**
- `langchain-google-genai`의 `ChatGoogleGenerativeAI`가 반환하는 `AIMessage.content`가 멀티모달 형식의 리스트로 반환됨
- Gemini API는 응답을 `[{"type": "text", "text": "...", "extras": {...}}]` 형태로 반환
- `react_chat_graph.py`의 `invoke()` 메서드에서 `last_message.content`를 그대로 반환하여 리스트가 문자열로 변환됨

**위치:** `service/react_chat_graph.py:180`
```python
return {
    "text": last_message.content,  # 문제: content가 list일 수 있음
    ...
}
```

**해결 방안:**
- `content`가 리스트인 경우 텍스트만 추출하는 전처리 필요
```python
def _extract_text_content(content):
    if isinstance(content, str):
        return content
    if isinstance(content, list):
        texts = []
        for item in content:
            if isinstance(item, dict) and item.get("type") == "text":
                texts.append(item.get("text", ""))
            elif isinstance(item, str):
                texts.append(item)
        return "".join(texts)
    return str(content)
```

---

### 1.2 ReAct 루프 제한 없음

**증상:**
- 툴 실행이 한두 번만 돌고 종료
- 복잡한 질문에 여러 툴을 순차적으로 사용하지 못함

**원인:**
- `builder.compile()`에 `recursion_limit` 미설정
- LangGraph 기본값이 낮거나, 조기 종료 조건에 걸림

**위치:** `service/react_chat_graph.py:107`
```python
self._graph = builder.compile(checkpointer=self._checkpointer)
# recursion_limit 미설정
```

**해결 방안:**
1. `ReActChatGraphBuilder.__init__`에 `max_iterations` 파라미터 추가 (기본값: 5)
2. `build()` 메서드에서 `recursion_limit` 설정
```python
self._graph = builder.compile(
    checkpointer=self._checkpointer,
    interrupt_before=None,
    interrupt_after=None,
)
# config에서 recursion_limit 설정
config = {"configurable": {"thread_id": session_id}, "recursion_limit": self.max_iterations}
```

3. 사이드바에 "Max Tool Iterations" 슬라이더 추가 (1-10, 기본값 5)

---

### 1.3 추론 툴(switch_to_reasoning) 미구현

**증상:**
- 복잡한 분석/추론 질문에 대해 `gemini-2.5-pro` 모델로 전환되지 않음
- 기존 `ChatGraphBuilder`에서 제공하던 추론 모드 기능 없음

**원인:**
- `service/tools.py`에 `switch_to_reasoning` 툴이 정의되지 않음
- 기존 키워드 기반 툴 선택 로직이 ReAct 패턴으로 대체되면서 누락

**기존 구현 (chat_graph.py):**
```python
# 추론 관련 키워드
reasoning_keywords = ["분석", "비교", "왜", "어떻게", "계산", "설명"]
if any(kw in user_input for kw in reasoning_keywords):
    selected.append("switch_to_reasoning")
```

**해결 방안:**
ReAct 패턴에서는 두 가지 접근 방법 가능:

**방법 A: 추론 툴 추가** (권장)
```python
@tool
def switch_to_reasoning() -> str:
    """복잡한 분석, 비교, 추론이 필요한 질문에 사용합니다.

    다음과 같은 질문에 이 도구를 사용하세요:
    - "왜", "어떻게" 로 시작하는 복잡한 질문
    - 여러 정보를 종합해야 하는 분석
    - 비교/대조가 필요한 질문
    - 수학적 계산이나 논리적 추론
    """
    return "추론 모드가 활성화되었습니다. 단계별로 사고하여 답변을 작성합니다."
```
- 이 툴이 호출되면 후속 LLM 호출 시 `gemini-2.5-pro` 사용

**방법 B: 시스템 프롬프트에서 처리**
- 복잡한 질문 감지 시 모델을 동적으로 전환
- 단점: LangGraph ReAct 루프 중간에 모델 변경이 어려움

---

### 1.4 RAG 툴(search_pdf_knowledge) 실행 문제

**증상:**
- PDF가 업로드되어 있어도 `search_pdf_knowledge` 툴이 호출되지 않음
- 문서 관련 질문에도 웹 검색만 수행

**원인 분석:**

1. **툴 설명 불충분**
   - 현재 설명: `"PDF, 문서 관련 질문에 사용합니다."`
   - LLM이 언제 이 툴을 사용해야 하는지 명확하지 않음

2. **PDF 컨텍스트 미전달**
   - 시스템 프롬프트에 현재 업로드된 PDF 정보가 없음
   - LLM이 PDF가 있다는 것을 모름

3. **툴 우선순위 문제**
   - `web_search`와 `search_pdf_knowledge` 중 LLM이 `web_search`를 선호
   - PDF 관련 질문도 웹 검색으로 처리하려 함

**위치:**
- `service/tools.py:38-56` (툴 설명)
- `service/react_chat_graph.py:56-72` (시스템 프롬프트)

**해결 방안:**

1. **툴 설명 개선**
```python
@tool
def search_pdf_knowledge(query: str) -> str:
    """현재 세션에 업로드된 PDF 문서에서 관련 내용을 검색합니다.

    반드시 이 도구를 사용해야 하는 경우:
    - 사용자가 "문서", "PDF", "파일", "업로드한" 등의 단어를 사용할 때
    - 사용자가 특정 문서의 내용에 대해 질문할 때
    - "문서에서 찾아줘", "파일에 뭐라고 써있어?" 등의 요청

    웹 검색(web_search) 대신 이 도구를 우선 사용하세요.
    """
```

2. **시스템 프롬프트에 PDF 정보 포함**
```python
def _get_system_prompt_with_context(self, pdf_description: str = "") -> str:
    base_prompt = self._get_default_system_prompt()
    if pdf_description:
        pdf_context = f"""

[업로드된 PDF 문서]
{pdf_description}

이 PDF와 관련된 질문이 있으면 반드시 search_pdf_knowledge 도구를 사용하세요.
"""
        return base_prompt + pdf_context
    return base_prompt
```

3. **_call_model에서 동적 시스템 프롬프트 적용**
```python
def _call_model(self, state: ChatState) -> dict:
    messages = state["messages"]
    pdf_description = state.get("pdf_description", "")

    # PDF 정보를 포함한 시스템 프롬프트 생성
    system_prompt = self._get_system_prompt_with_context(pdf_description)

    if not messages or not isinstance(messages[0], SystemMessage):
        system_msg = SystemMessage(content=system_prompt)
        messages = [system_msg] + list(messages)

    response = self._llm_with_tools.invoke(messages)
    return {"messages": [response]}
```

---

## 2. 구현 우선순위

| 순위 | 문제 | 심각도 | 해결 난이도 |
|------|------|--------|------------|
| 1 | 텍스트 깨짐 | 🔴 Critical | ⭐ 쉬움 |
| 2 | RAG 툴 미실행 | 🟠 High | ⭐⭐ 보통 |
| 3 | ReAct 루프 제한 | 🟡 Medium | ⭐ 쉬움 |
| 4 | 추론 툴 미구현 | 🟡 Medium | ⭐⭐ 보통 |

---

## 3. 사이드바 설정 추가 항목

```
🔧 Agent Settings
├── Max Tool Iterations: [1-10, 기본값: 5]
└── (향후) Enable Reasoning Mode: [체크박스]
```

---

## 4. 구현 체크리스트

- [x] 텍스트 콘텐츠 추출 함수 구현 (`extract_text_content`)
- [x] `invoke()` 반환값에서 텍스트 전처리 적용
- [x] `max_iterations` 파라미터 추가 (기본값: 5)
- [x] 사이드바에 "Max Tool Iterations" 슬라이더 추가
- [x] `search_pdf_knowledge` 툴 설명 개선
- [x] 시스템 프롬프트에 PDF 정보 동적 포함 (`_get_system_prompt_with_context`)
- [x] `switch_to_reasoning` 툴 구현 (`create_reasoning_tool`)
- [x] 단위 테스트 (12개 추가, 총 160개)
- [x] 통합 테스트 (8개, 실제 API 사용)

---

## 5. 구현 결과

### 변경된 파일
| 파일 | 변경 내용 |
|------|----------|
| `service/react_chat_graph.py` | `extract_text_content()`, `max_iterations`, `_get_system_prompt_with_context()` 추가 |
| `service/tools.py` | `create_reasoning_tool()`, 모든 툴 설명 개선 (적극적 사용 유도) |
| `prompt/system/agent.txt` | 툴 사용 적극 유도 시스템 프롬프트로 전면 개편 |
| `component/sidebar.py` | "Agent Settings" 섹션 추가 (Max Tool Iterations) |
| `app.py` | `max_iterations` 전달 |
| `tests/test_chat_graph.py` | Phase 02-3 테스트 12개 추가 |
| `tests/test_integration.py` | 실제 API 통합 테스트 8개 |

### 테스트 결과
- 단위 테스트: 160 passed
- 통합 테스트: 8 passed (실제 Gemini/Tavily API)
- **총 168개 테스트 통과**

---

## 6. 추가 개선 (툴 호출 적극화)

### 문제
- 시스템 프롬프트가 `"도구를 사용할 필요가 없다면 바로 답변하세요"` → LLM이 툴 사용을 피함
- 추론/RAG 툴이 거의 호출되지 않음

### 해결
1. **시스템 프롬프트 전면 개편** (`prompt/system/agent.txt`)
   - `"도구를 적극적으로 사용하세요"` 로 변경
   - 각 툴별 **반드시** 사용해야 하는 조건 명시
   - `"여러 도구를 순차적으로 사용할 수 있습니다"` 추가

2. **툴 설명 강화** (`service/tools.py`)
   - 모든 툴에 "반드시 사용해야 하는 경우" 리스트 추가
   - 다른 툴과의 연계 사용 권장 문구 추가

### 개선 결과
```
테스트 1: "파이썬과 자바 비교 분석해줘"
→ 툴 호출: [switch_to_reasoning] ✅

테스트 2: "LangGraph 최신 기능 검색하고 분석해줘"
→ 툴 호출: [web_search, switch_to_reasoning] ✅ (2개 순차 호출!)
```

---

## 7. 프롬프트 사용 현황 분석

### 7.1 prompt/ 디렉토리 구조
```
prompt/
├── system/
│   ├── agent.txt           # ✅ 활성 (ReActChatGraphBuilder)
│   ├── base.txt            # ⚠️ 레거시 (ChatGraphBuilder)
│   ├── pdf_extension.txt   # ⚠️ 레거시 (ChatGraphBuilder)
│   └── chain_of_thought.txt # ⚠️ 레거시 (ChatGraphBuilder)
├── search/
│   └── tavily_instruction.txt # ⚠️ 레거시 (ChatGraphBuilder)
├── summary/
│   └── summary.txt         # ⚠️ 미사용 (함수만 존재)
└── pdf/
    ├── normalization.txt   # ✅ 활성 (app.py PDF 처리)
    └── description.txt     # ✅ 활성 (app.py PDF 처리)
```

### 7.2 활성 프롬프트 (현재 사용 중)

| 파일 | 사용 위치 | 평가 |
|------|----------|------|
| `agent.txt` | `ReActChatGraphBuilder._get_default_system_prompt()` | ✅ 적극적 툴 사용 유도, 명확한 조건 제시 |
| `normalization.txt` | `app.py:219` (PDF 전처리) | ✅ 규칙 명확, 간결함 |
| `description.txt` | `app.py:224` (PDF 설명 생성) | ✅ 작성 지침 명확, 길이 제한 적절 |

### 7.3 레거시 프롬프트 (이전 버전용)

| 파일 | 이전 사용 위치 | 비고 |
|------|--------------|------|
| `base.txt` | `ChatGraphBuilder` (Phase 01) | ReAct 전환 후 미사용 |
| `pdf_extension.txt` | `ChatGraphBuilder` | `_get_system_prompt_with_context()`로 대체 |
| `chain_of_thought.txt` | `ChatGraphBuilder` | `switch_to_reasoning` 툴로 대체 |
| `tavily_instruction.txt` | `ChatGraphBuilder` | ReAct에서 직접 툴 호출로 대체 |
| `summary.txt` | `PromptLoader.get_summary_prompt()` | 함수 존재하나 실제 호출 없음 |

### 7.4 프롬프트 품질 검토

**agent.txt** (⭐⭐⭐⭐⭐) - Phase 02-3에서 확장
- 각 툴별 "반드시" 사용 조건 명시
- 툴 순차 사용 가능 안내
- PDF 우선 사용 지침 포함
- **[추가] 도구 결과 처리 지침** (web_search, search_pdf_knowledge, switch_to_reasoning)
- **[추가] 응답 형식 지침** (구조화된 답변, 답변 길이, 한국어 답변)
- 한국어 응답 명시

**normalization.txt** (⭐⭐⭐⭐)
- 5가지 규칙 명확
- 핵심 키워드 유지 강조
- 개선점: 예시 추가하면 더 좋음

**description.txt** (⭐⭐⭐⭐)
- 256자 길이 제한 적절
- 문서 주제/목적/핵심내용 요청
- 개선점: 출력 형식 예시 추가 가능

### 7.5 권장 조치

1. **레거시 프롬프트 정리**: `prompt/legacy/`로 이동 또는 삭제
2. **summary.txt 활성화**: 요약 기능에 실제 연결 필요
3. **prompts_tab.py**: 레거시 프롬프트 표시 시 "[Legacy]" 라벨 추가

---

## 8. Overview 탭 Mermaid 렌더링 수정

### 문제
```
Syntax error in text mermaid version 10.2.4
```

### 원인
- Mermaid 10.x에서 한글 subgraph 이름 미지원
- 노드 레이블의 이모지와 `<br/>` 태그 호환성 문제

### 해결
```python
# Before (에러)
subgraph 입력
    A[👤 사용자 입력]
end

# After (정상)
subgraph Input["입력"]
    A["사용자 입력"]
end
```

### 변경 파일
- `component/overview_tab.py`: `get_langgraph_diagram()`, `get_response_length_diagram()` 수정
- `prompt/system/agent.txt`: 도구 결과 처리 지침 및 응답 형식 지침 추가
